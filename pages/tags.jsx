import BlogLayout from "layout/Blog"
import layout from "lib/hoc/layout"

/** @type {React.FC} */
const Tags = () => (
  <div>Tags index will be here</div>
)

export default Tags |> layout(BlogLayout)
