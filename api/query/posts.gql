query GetPosts($page: Int) {
  posts(page: $page) {
    list {
      id
      title
      slug
      text
      isDraft

      creator {
        id
        login
        name {
          first
          last
        }
      }

      dates {
        createdAt
        updatedAt
        deletedAt
      }
    }
  }
}
