html
  head
    meta(charset="utf-8")
    meta(name="viewport", content="user-scalable=no, width=device-width, initial-scale=1, maximum-scale=1")
    meta(name="generator", content="Eri v" + version)
    link(rel="stylesheet", href="/assets/css/http.css")
    title
      block title
  body
    .err-container
      .err-image
        block image
      .err-status= status
      .err-message
        block message
          | I just don't know what went wrong
      if status < 500
        .err-link
          a(href="/") Home
